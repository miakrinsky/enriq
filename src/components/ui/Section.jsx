function Section({ children }) {
  return <div>{children}</div>;
}

export default Section;

// כשאת בונה אתר, כל סקשן צריך:

// Padding אחיד (ריווח פנימי)
// Container שמרכז את התוכן
// רוחב מקסימלי (שלא יתפרס על כל המסך הענק)
